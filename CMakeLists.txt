cmake_minimum_required(VERSION 2.6)

project( SegmentationToGoFigure2 )

FIND_PACKAGE( VTK )
IF( VTK_FOUND )
  INCLUDE( ${VTK_USE_FILE} )
ENDIF( VTK_FOUND )

FIND_PACKAGE( ITK )
IF( ITK_FOUND )
  INCLUDE( ${ITK_USE_FILE} )
ENDIF( ITK_FOUND )

ADD_SUBDIRECTORY( Source )

